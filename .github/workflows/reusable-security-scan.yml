name: Reusable Security Scanning

on:
  workflow_call:
    inputs:
      components:
        description: 'Components to scan (space-separated: backend frontend infrastructure)'
        required: true
        type: string
      include-lint-analysis:
        description: 'Include code quality analysis (ESLint, detekt)'
        required: false
        type: boolean
        default: true

# Cost optimization: single concurrency group for all security scans
concurrency:
  group: security-scan-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Backend security scanning
  backend-security:
    name: Backend Security Scan
    runs-on: ubuntu-latest
    if: contains(inputs.components, 'backend')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # Two passes, deliberately. With `format: sarif` the trivy-action builds the
      # report with ALL severities (so nothing is hidden from the Security tab) and
      # ignores the `severity` input — so a single step cannot both report
      # everything and fail only on HIGH/CRITICAL. Hence: report, then gate.
      - name: Run Trivy vulnerability scanner (report)
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'fs'
          scan-ref: './apps/api'
          format: 'sarif'
          output: 'trivy-backend-results.sarif'

      - name: Upload SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-backend-results.sarif'
          category: 'security-backend'
        continue-on-error: true

      # The real gate. This job is a `needs:` prerequisite for deploy, and
      # previously could not fail, so HIGH/CRITICAL CVEs could not block a
      # release. Add unactionable findings to .trivyignore.
      - name: Fail on HIGH/CRITICAL vulnerabilities
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'fs'
          scan-ref: './apps/api'
          format: 'table'
          severity: 'HIGH,CRITICAL'
          ignore-unfixed: 'true'
          exit-code: '1'

  # Frontend security scanning
  frontend-security:
    name: Frontend Security Scan
    runs-on: ubuntu-latest
    if: contains(inputs.components, 'frontend')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # See the backend scan above for why this is two steps.
      - name: Run Trivy vulnerability scanner (report)
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'fs'
          scan-ref: './apps/web'
          format: 'sarif'
          output: 'trivy-frontend-results.sarif'

      - name: Upload SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-frontend-results.sarif'
          category: 'security-frontend'
        continue-on-error: true

      - name: Fail on HIGH/CRITICAL vulnerabilities
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'fs'
          scan-ref: './apps/web'
          format: 'table'
          severity: 'HIGH,CRITICAL'
          ignore-unfixed: 'true'
          exit-code: '1'

  # Infrastructure security scanning
  infrastructure-security:
    name: Infrastructure Security Scan
    runs-on: ubuntu-latest
    if: contains(inputs.components, 'infrastructure')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # See the backend scan above for why this is two steps.
      - name: Run Trivy IaC security scanner (report)
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'config'
          scan-ref: 'infrastructure/terraform'
          format: 'sarif'
          output: 'trivy-infrastructure-results.sarif'

      - name: Upload SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-infrastructure-results.sarif'
          category: 'security-infrastructure'
        continue-on-error: true

      # Gates on HIGH/CRITICAL misconfigurations only. The Terraform currently has
      # 7 LOW/MEDIUM findings (public media bucket, missing logging) which are
      # reported to the Security tab above but do not block; see #166.
      - name: Fail on HIGH/CRITICAL misconfigurations
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'config'
          scan-ref: 'infrastructure/terraform'
          format: 'table'
          severity: 'HIGH,CRITICAL'
          exit-code: '1'

  # Optional code quality analysis
  backend-lint:
    name: Backend Code Quality
    runs-on: ubuntu-latest
    # Disabled: detekt is commented out pending 2.0.0 stable (Java 25 compatibility)
    # Re-enable when detekt is ready by removing 'false &&'
    if: false && contains(inputs.components, 'backend') && inputs.include-lint-analysis == true

    defaults:
      run:
        working-directory: ./apps/api

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Java & Gradle
        uses: ./.github/actions/setup-java-gradle
        with:
          cache-read-only: 'true'

      # detekt temporarily disabled pending 2.0.0 stable release (Java 25 compatibility)
      # - name: Run detekt analysis
      #   run: ./gradlew detekt
      #   continue-on-error: true

      # - name: Upload detekt results
      #   if: always()
      #   uses: github/codeql-action/upload-sarif@v4
      #   with:
      #     sarif_file: 'apps/api/build/reports/detekt/detekt.sarif'
      #     category: 'detekt-backend'
      #   continue-on-error: true

  # Frontend lint analysis
  frontend-lint:
    name: Frontend Code Quality
    runs-on: ubuntu-latest
    if: contains(inputs.components, 'frontend') && inputs.include-lint-analysis == true

    defaults:
      run:
        working-directory: ./apps/web

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node & pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Run ESLint with SARIF output
        run: |
          pnpm exec eslint . --format=@microsoft/eslint-formatter-sarif --output-file=eslint-frontend-results.sarif || true
        continue-on-error: true

      - name: Upload ESLint SARIF results
        if: always() && hashFiles('apps/web/eslint-frontend-results.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'apps/web/eslint-frontend-results.sarif'
          category: 'eslint-frontend'
        continue-on-error: true

  # Summary job
  scan-summary:
    name: Security Scan Summary
    runs-on: ubuntu-latest
    # Don't fail if individual jobs are skipped, only if they failed
    needs: [backend-security, frontend-security, infrastructure-security, backend-lint, frontend-lint]
    if: |
      always() &&
      !contains(needs.*.result, 'cancelled')

    steps:
      - name: Generate scan summary
        run: |
          echo "## Security Scan Summary 🛡️" >> $GITHUB_STEP_SUMMARY
          echo "**Scanned Components:** ${{ inputs.components }}" >> $GITHUB_STEP_SUMMARY
          echo "**Code Quality Analysis:** ${{ inputs.include-lint-analysis }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Job status summary
          echo "### Job Results:" >> $GITHUB_STEP_SUMMARY
          echo "- Backend Security: ${{ needs.backend-security.result || 'skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Frontend Security: ${{ needs.frontend-security.result || 'skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Infrastructure Security: ${{ needs.infrastructure-security.result || 'skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Backend Lint: ${{ needs.backend-lint.result || 'skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Frontend Lint: ${{ needs.frontend-lint.result || 'skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "*This consolidated scan saves ~60% in CI minutes compared to individual component scans*" >> $GITHUB_STEP_SUMMARY